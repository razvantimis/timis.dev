---
const { title } = Astro.props;
---

<section class="flex flex-row mt-4 items-start">
  <h2 class="bg-black p-2 h-auto flex-grow-0 text-white text-xl w-52 font-bold">{title}</h2>
  <div class="flex flex-col ml-6 flex-1">
    <slot />
  </div>
</section>
