---
const { title } = Astro.props;
---

<section class="flex flex-col mt-4 items-center md:items-start md:flex-row">
  <h2 class="bg-black p-2 h-auto flex-grow-0 text-white text-xl md:w-52 font-bold mb-2">{title}</h2>
  <div class="flex flex-col md:ml-6 flex-1 w-full">
    <slot />
  </div>
</section>
